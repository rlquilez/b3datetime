# Dependências de desenvolvimento e CI.
# Instale com:  pip install -r requirements-dev.txt
-r requirements.txt

# --- testes ---
pytest==8.4.2
# pytest-asyncio 0.25 renomeou as chaves de loop-scope; 0.24 + asyncio_mode=auto é estável.
pytest-asyncio==0.24.0
pytest-cov==7.1.0
coverage[toml]==7.15.4
# httpx >=0.28 removeu o atalho ASGITransport(app=...) e o TestClient do
# FastAPI 0.115 espera <0.28. Não flutuar este pin.
httpx==0.27.2
asgi-lifespan==2.1.0
fakeredis==2.37.0
freezegun==1.5.5
pytest-randomly==4.1.0

# --- qualidade ---
ruff==0.16.3
mypy==2.3.1
types-pytz==2026.3.1.20260727

# --- segurança ---
# O formatter SARIF do bandit é um extra; sem ele `-f sarif` falha.
bandit[toml,sarif]==1.9.4
pip-audit==2.10.1
